import ReserveLayout from "@/app/components/layout/ReserveLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <ReserveLayout>{children}</ReserveLayout>;
}
